 {/* <Card cardItem={cardItem} cat={item.category} /> */}
        {/* .map((item).filter(type => type.categoryName === item.category)=>{ */}
        // eslint-disable-next-line no-lone-blocks
        {/* .filter(type => type.categoryName === item.category).map((item) =>{ */}
        // {item.details.map((subItem, index) => {
                    
        //     return (
        //       <div className="card-container" key={index}>
        //         <img
        //           src={subItem.img}
        //           alt="example"
        //           className="card-img"
        //         />
        //         <h2>{subItem.title}</h2>
        //         <p className="category">{subItem.subcategory}</p>

        //         <Link to={`/projectview/${subItem.index}`}>
        //           <button className="card-btn">Veiw</button>
        //         </Link>
        //       </div>
        //     );
          
        // })}